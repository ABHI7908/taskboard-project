receivers:
  - name: 'slack-notifications'
    slack_configs:
      - api_url: 'https://hooks.slack.com/services/XXX/YYY/ZZZ'
        channel: '#taskboard-alerts'
        send_resolved: true

route:
  receiver: 'slack-notifications'
